import SwiftUI

extension Color {
    // Primary gradient endpoints
    static let hulunotePurpleStart = Color(hex: "667EEA")
    static let hulunotePurpleEnd = Color(hex: "764BA2")

    // Dark theme backgrounds
    static let hulunoteBackground = Color(hex: "313541")
    static let hulunoteSidebar = Color(hex: "2E3340")
    static let hulunoteCard = Color.white.opacity(0.05)
    static let hulunoteCardHover = Color.white.opacity(0.10)
    static let hulunoteDarkNavy = Color(hex: "1A1A2E")

    // Text
    static let hulunoteTextPrimary = Color(hex: "FDFEFFC4")
    static let hulunoteTextSecondary = Color(hex: "8A8EA0")
    static let hulunoteTextMuted = Color(hex: "666666")

    // Accent
    static let hulunoteAccent = Color(hex: "4A90D9")
    static let hulunoteSuccess = Color(hex: "98C379")

    // Borders & Dividers
    static let hulunoteBorder = Color.white.opacity(0.1)
    static let hulunoteTag = Color(hex: "667EEA").opacity(0.3)

    // Input
    static let hulunoteInputBorder = Color(hex: "E0E0E0")
    static let hulunoteInputBackground = Color.white.opacity(0.08)
}
